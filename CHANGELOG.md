# Changelog

All notable changes to AgentSpec packages are documented in each package's `CHANGELOG.md`, generated by Changesets.

This root file tracks repo-level milestones.

## Unreleased

- Milestone 0: monorepo scaffolding, core types, first rule (`conflict/binding`), minimal CLI (`lint` subcommand only).
