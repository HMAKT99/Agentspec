---
"@agentspec/web": minor
---

Milestone 4 — part 1: Next.js 15 scaffold + landing page.

- New `apps/web` Next.js 15 app (App Router, React 19, Tailwind v4).
- Design tokens per spec §12 (warm off-white background, single accent orange, hairline borders, no gradients or shadows).
- Landing page: header, hero, terminal demo, before/after snippet, feature grid, footer. Light-mode only.
- Routes for `/docs`, `/rules`, `/playground` are linked from the header; the actual pages land in subsequent PRs.
