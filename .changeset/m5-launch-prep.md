---
"agentspec": patch
---

Milestone 5: launch prep.

- Root `README.md` rewritten end-to-end: positioning, 60-second tour with real CLI output, privacy posture, package index, Action snippet.
- `vercel.json` at repo root + `apps/web/README.md` documenting the pnpm + Turbo deploy model. Vercel project points at `apps/web` and the rest is config-driven.
- `marketing/` contains launch assets staged for Show HN, the "Your CLAUDE.md is probably broken" blog post, and an X thread.

Alpha publish to npm and Marketplace stays gated on the package rename tracked in `TODO-RENAME.md`.
