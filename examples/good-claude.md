---
version: 1
owner: platform-team
scope: example
---

# CLAUDE.md (good example)

This file passes `conflict/binding` — binding rules are internally consistent and exceptions are explicit.

## Commit discipline

- You must always commit before pushing.
- Unless the branch is a scratch branch, in which case pushing without a commit is fine.

## Style

- Always use tabs for indentation.
- Prefer single quotes in JavaScript.

## Tools

- You should prefer `rg` over `grep` when searching the repo.
